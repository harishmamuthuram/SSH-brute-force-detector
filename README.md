# SSH-brute-force-detector
A Python-based SSH Brute Force Log Detector that simulates SSH login attempts, records authentication activity in CSV log files, identifies suspicious failed login patterns, assigns risk levels, and generates security reports for potential brute-force attacks.
